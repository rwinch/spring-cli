actions:
#  - if: "#{  ! ['execUtil'].exec('input').contains( ['John'] ) }"
  - if: "#{  (['execUtil'].execCommandFile('echo-command.txt')).contains( 'John' )  }"
    vars:
      data:
        name: "John is present in the output"
